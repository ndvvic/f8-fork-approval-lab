name: pr-check
on:
  pull_request:
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: marker
        run: |
          echo "F8-FORK-CODE-EXECUTED-ATTACKER"
          echo "actor=$GITHUB_ACTOR repo=$GITHUB_REPOSITORY event=$GITHUB_EVENT_NAME"
